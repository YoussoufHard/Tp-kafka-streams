spring:
  application:
    name: ClickStreamsApp
  kafka:
    bootstrap-servers: localhost:9092
    streams:
      application-id: click-streams-app
      properties:
        commit.interval.ms: 1000
        auto.offset.reset: earliest
server:
  port: 8082